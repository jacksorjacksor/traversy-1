# Deployment guide

## Install the Netlify CLI
```bash
$ npm i -g netlify-cli
```
(if on mac that needs to be sudo)

## Create build
```bash
$ npm run build
```

## Deploy build
```bash
$ netlify deploy --prod
```